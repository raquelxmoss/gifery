.pure-g
  - gifs.in_groups_of(3,false) do |group|
    - group.each do |gif|
      .pure-u-1-3
        - if gif.is_gifv?
          = video_tag(gif.url, autoplay: true, loop: true)
        - else
          =image_tag gif.url, class: 'pure-img'